{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "installCommand": "npm install --include=optional",
  "buildCommand": "npm run build --workspace=web && rm -rf .next && ln -sfn apps/web/.next .next"
}
