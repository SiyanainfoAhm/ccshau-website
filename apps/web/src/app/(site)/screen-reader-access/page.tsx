import { PublicScreenReaderAccessPage } from "@/components/site/public-screen-reader-access-page";

export const metadata = {
  title: "Screen Reader Access",
  description:
    "How a blind visitor can use a screen reader on the CCSHAU Hisar website, including Narrator, VoiceOver, and TalkBack.",
};

export default function ScreenReaderAccessPage() {
  return <PublicScreenReaderAccessPage />;
}
