import { DesignGallery } from "@/components/design/design-gallery";

export const metadata = {
  title: "UI/UX Design Gallery — Phase 1",
};

export default function DesignGalleryPage() {
  return <DesignGallery />;
}
