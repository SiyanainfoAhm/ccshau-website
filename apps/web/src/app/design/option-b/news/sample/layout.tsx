import type { Metadata } from "next";

export const metadata: Metadata = { title: "Online Admission 2026-27" };

export default function NewsDetailLayout({ children }: { children: React.ReactNode }) {
  return children;
}
